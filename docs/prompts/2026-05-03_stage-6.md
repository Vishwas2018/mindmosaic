# Stage 6 C-C-D-V Prompt — 2026-05-03

## Context

Stage 6 of 75: Migration 0005 — Intelligence Foundation (L1 Foundation Layer).
12 tables from arch §2.8–§2.10: skill_mastery, learning_velocity, behaviour_profile,
student_misconception, repair_record, intelligence_audit_log (partitioned monthly),
learning_plan, plan_revision, recommendation, plan_override, intervention_alert,
cohort_metric_cache.

Preceding state: 240/240 pgTAP green (28 tables RLS-tested). All quality gates green.

§2A pre-implementation review produced five decisions (D1–D4 + 2 clarifications):
- D1: intelligence_audit_log → Pattern A (row-level) + app-layer column redaction (ADR-0013).
  NOT Pattern G. intelligence-svc (Stage 18+) enforces column projection.
- D2: plan_revision → Pattern G (service_role only). No authenticated access.
- D3: cohort_metric_cache → add tenant_id (not in arch §2.10); selective grant to
  teacher/org_admin/platform_admin with tenant isolation (overrides arch §3.2 Pattern G);
  tenant_id in PRIMARY KEY to prevent cross-tenant cohort_key conflicts.
- D4: no student SELECT on intervention_alert in v1. Teacher access via
  teacher_id = auth_user_id() (direct column; no fn_teacher_student_ids recursion).
- CLARIFICATION 1: intelligence_audit_log default partition only in v1.
- CLARIFICATION 2: intelligence_audit_log PRIMARY KEY (id, created_at) per ADR-0012.

Execution directives:
- X1: intervention_alert.teacher_id nullability → confirmed NOT NULL from arch grep.
- X2: Partition INSERT test must prove DEFAULT partition routing; use created_at='2099-01-01'.
- X3: G4 guard exact RAISE message read from Migration 0002:
  'PUBLISH_BLOCKED: downstream student data exists; run skill_graph_migration worker
  before publishing a new graph version'. Stable pattern: '%PUBLISH_BLOCKED%'.

## Constraints

- BUILD_CONTRACT §6: triple REVOKE (PUBLIC × 2 + anon) on all new SECURITY DEFINER functions.
  No new SECURITY DEFINER functions in Stage 6 (helpers reused from Migration 0004 per ADR-0011).
- ADR-0012: partitioned table PK must include partition key column (created_at for intelligence_audit_log).
- ADR-0013: audit table column redaction is app-layer responsibility (intelligence-svc Stage 18+).
- All RLS policies in same migration as CREATE TABLE (never follow-up migration).
- plan(N) target was plan(70) after §2A adjustments and discovered patterns:
  - Anon SELECT tests removed: SECURITY DEFINER helpers REVOKE'd from anon; policy evaluation fails.
  - INSERT deny tests: throws_like pattern (INSERT raises RLS error, not silent 0-row return).
  - 002_content.sql G11 stub removed: skill_mastery now real table (ADR-0007 transfer to G_G4).

## Deliverables

1. `supabase/migrations/0005_intelligence_orchestration.sql` — 12 tables, 7 triggers, C7 indexes,
   intelligence_audit_log partition, full RLS section implementing D1–D4.
2. `supabase/migrations/down/0005_intelligence_orchestration.down.sql` — DROP 12 tables in reverse
   FK dependency order.
3. `supabase/tests/rls/005_intelligence_orchestration.sql` — plan(70), 18 groups:
   G2–G11 (Pattern A × 10, 6 tests each), G12 (plan_revision Pattern G, 2 tests),
   G13 (cohort_metric_cache, 3 tests), G_G4 (1), G_C7 (2), G_part (2).
4. `supabase/tests/rls/002_content.sql` — plan(40)→plan(38); G11 stub block removed.

## Verification

```
supabase db reset
pnpm test:migration   # roundtrip up→down→up
pnpm test:rls         # 308/308 cumulative
pnpm turbo typecheck lint test  # 18 packages green
Manual smoke: intelligence_audit_log Pattern A — 2 students, student-1 JWT sees only student-1 rows
```

All verification passed. Stage commit: 2343cce.

## Bugs found during implementation

- `now()` in index predicate → IMMUTABLE required; replaced with column-in-index pattern.
- Nested data-modifying CTE in subquery → invalid; replaced with top-level throws_like.
- Anon + SECURITY DEFINER helper policies → permission denied; removed anon SELECT tests;
  use has_function_privilege pattern (Stage 4 G16 precedent).
- 002_content.sql G11 stub → skill_mastery now real table; stub fails; G11 removed (ADR-0007).
