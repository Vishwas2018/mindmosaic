# Stage 7 C-C-D-V Prompt — 2026-05-03

## Context

Stage 7 of 75: Migration 0006 — Jobs + Outbox + Rate Limit (infra layer).
Three new tables (job_queue, pipeline_event, rate_limit_bucket). All Pattern G
(service_role only; RLS enabled, zero policies). outbox_event already created
in migration 0004 — excluded from Stage 7 scope.

Preceding state: 308/308 pgTAP green (39 tables). All quality gates green.

§2A pre-implementation review confirmed all decisions before code:
- C1: Stage 7 scope confirmed — 3 tables (job_queue, pipeline_event, rate_limit_bucket).
  outbox_event excluded (already in 0004).
- C2: User-supplied §2A pre-cues (i)–(v) deferred to Stage 8; Stage 7 is Jobs/Outbox.
- C3: Cleanest stage from policy standpoint — mechanical Pattern G, no new helpers.
- D1 (ADR-0014): EXPLAIN deferred; use pg_indexes catalog check + P5 dedup for indexes.

## Constraints

- Pattern G: all three tables deny all authenticated; service_role bypasses RLS.
- ADR-0014: structural pg_indexes catalog check + P5 dedup; no EXPLAIN assertions.
- No new SECURITY DEFINER functions (Pattern G requires none).
- No new enum types (job_priority, job_status, pipeline_step_status defined in 0001).
- Partial index predicates must be IMMUTABLE (BUILD_CONTRACT §10 item 6).
- Session_record.assignment_id FK deferred to Stage 8 (0004 header note).

## Deliverables

1. `supabase/migrations/0006_jobs_outbox_rate_limit.sql` — 3 tables, 7 indexes,
   RLS enabled + 0 policies for all three.
2. `supabase/migrations/down/0006_jobs_outbox_rate_limit.down.sql` — DROP 3 tables.
3. `supabase/tests/rls/006_jobs_outbox_rate_limit.sql` — plan(26), 6 groups:
   G1 job_queue (6: RLS+auth0row+anon0row+INSERT deny+dedup G1.4-G1.6),
   G2 pipeline_event (4), G4 rate_limit_bucket (6: 4+PK dedup G4.5-G4.6),
   G_idx (7: catalog checks all Stage 7 indexes), G_meta (3: 0-policy checks).
4. `docs/dev/decisions/0014-pgtap-index-assertions-structural-not-explain.md`

## Verification

```
pnpm test:migration   # roundtrip up→down→up
pnpm test:rls         # 334/334 cumulative (308 + 26 Stage 7 tests)
pnpm turbo typecheck lint test  # 18 packages green (all cached)
```

All verification passed.

## Surprises / scope adjustments

- outbox_event already in migration 0004 (line 183). DEV_PLAN Stage 7 listed it;
  arch §2.15 lists it. Removed from 0006 scope. No deviation filed (stage-plan
  mismatch, not a build deviation).
- plan(26) not plan(30) (X3 target): outbox_event removal = -4 tests.
  Cumulative: 308 + 26 = 334, not 338.
- idx_job_dedup X3 3-subtest proof (G1.4 lives_ok / G1.5 throws_like '%duplicate key%' /
  G1.6 lives_ok with status='completed') confirmed partial predicate honored.
- ADR-0014 slug error: initially filed as `catalog-not-explain.md` (untracked, not
  committed). Correct file: `structural-not-explain.md`. Stub at catalog path left
  untracked; clean up before Stage 10 audit.
