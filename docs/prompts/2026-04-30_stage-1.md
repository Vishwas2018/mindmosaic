# Stage 1 C-C-D-V Prompt — 2026-04-30

## Executed prompt

```
Stage 1 — Monorepo & Tooling
Reference: DEV_PLAN.md §2 Stage 1; BUILD_CONTRACT §2, §3, §8, §10, §11.2;
spec §4 PR 0.1; Arch §8.2.

# CONTEXT
- Current phase: Phase 0 — Foundations (Days 1–17, 3-day slippage buffer)
- Day in plan: Day 1
- What exists already: bare git repo containing only docs/ (planning files).
  No package.json, no node_modules, no application code.
- What this stage is NOT doing:
  - No Supabase migrations (Stage 2+)
  - No packages/engines-client (deferred to Stage 15, DEV-20260430-1, ADR-0001)
  - No application routes, API handlers, or UI components
  - No Supabase Edge Functions
  - No seed data
  - No real migration dry-run (CI job is a stub; wired for Stage 2)

# CONSTRAINTS
- BUILD_CONTRACT §2: stack is Next.js 14 App Router, TypeScript strict,
  pnpm workspaces, Node >=20 + pnpm >=9 pinned.
- BUILD_CONTRACT §3: tsconfig must have strict, noUncheckedIndexedAccess,
  noImplicitOverride, noPropertyAccessFromIndexSignature all true. Zero any.
- BUILD_CONTRACT §10: CI required gates — lint, typecheck, unit, migration-dryrun.
- BUILD_CONTRACT §11.2: commits must use the .gitmessage template format.
- G2: zero Stripe code anywhere.
- packages/engines-client must NOT be created here (ADR-0001).

# DELIVERABLES
1. turbo.json — tasks: build (dependsOn ^build), typecheck, lint, test, dev
2. pnpm-workspace.yaml — globs: apps/*, packages/*
3. package.json (root) — engines >=20/>=9, packageManager pnpm@10.30.3,
   prepare: husky, pnpm.onlyBuiltDependencies for esbuild/unrs-resolver
4. tsconfig.base.json — all BUILD_CONTRACT §3 strict flags
5. apps/web/ — Next.js 14.2 App Router (TypeScript, Tailwind 3)
6. packages/{types,sdk,ui,core,engines}/ — each with package.json,
   tsconfig.json (NodeNext), src/index.ts (export {})
7. .eslintrc.json (root) — @typescript-eslint/recommended + no-explicit-any
8. apps/web/.eslintrc.json — next/core-web-vitals + no-explicit-any
9. .prettierrc + .prettierignore
10. .husky/pre-commit — pnpm turbo run typecheck lint
11. .github/workflows/ci.yml — lint / typecheck / unit / migration-dryrun
12. .gitmessage — BUILD_CONTRACT §11.2 format
13. .npmrc — shamefully-hoist=true, auto-install-peers=true
14. docs/dev/decisions/0001-engines-client-deferred.md (ADR-0001)
15. docs/dev/DEVIATIONS.md — DEV-20260430-1 entry

# VERIFICATION
1. pnpm install → exit 0 ✅
2. pnpm turbo run build → 6/6 green ✅ (23s, Next.js 14.2.35 build included)
3. pnpm turbo run typecheck → 6/6 green ✅ (3s)
4. pnpm turbo run lint → 6/6 green ✅ (4.7s)
5. pnpm turbo run test → 6/6 green ✅ (2.8s, pass-with-no-tests)
6. git config commit.template → .gitmessage ✅
7. .husky/pre-commit → mode 100755 ✅
```

## Outcome notes

- Tool versions: Node 24.12.0 (satisfies >=20), pnpm 10.30.3 (satisfies >=9),
  Supabase CLI 2.96.0, Docker 29.3.1.
- esbuild native binary blocked by pnpm 10 build script policy; resolved by
  adding `pnpm.onlyBuiltDependencies` to root package.json.
- Vite CJS deprecation warning in vitest is cosmetic; vitest still exits 0.
- .npmrc uses targeted `public-hoist-pattern[]=*eslint*/*prettier*/typescript/*vitest*`
  (not shamefully-hoist). Set proactively; narrowed during pre-push review. See ADR-0002.
- packages/engines-client not created; ADR-0001 + DEV-20260430-1 filed.
- Next.js resolved to 14.2.35 (latest 14.2.x patch at install time).
