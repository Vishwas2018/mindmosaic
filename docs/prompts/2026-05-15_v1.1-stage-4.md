# C-C-D-V Prompt — v1.1-S4 Teacher Exam Authoring UI

**Date:** 2026-05-15  
**Stage:** v1.1-S4  
**Branch:** v1.1/exam-content  
**Budget:** ~2 days

---

## Context

MindMosaic v1.1-S4: Teacher Exam Authoring UI. First UI stage of v1.1. T5 layout discipline
activates (sketch → skeleton → fill; two architect checkpoints before component code is written).

Completed prior stages on this branch:
- v1.1-S1: Question Bank CRUD API (content-svc) — 9376d98 baseline → 3 commits
- v1.1-S2: Practice Exam Composer API (assessment-svc + types) — `composer_params` additive on
  `CreateSessionRequest`; ADR-0036 accepted
- v1.1-S3: Simulation Exam Mode (types, engines, assessment-svc) — `simulation_params` additive
  on `CreateSessionRequest`; ADR-0037 accepted; tests 753→770

S4 consumes S1 (bank, read-only via `usePathways()`) + S2 (`PracticeExamComposerParamsSchema`).
Five Q-1.1-4.* items resolved (see QUESTIONS.md ## Resolved). ADR-0038 proposed.

**§N trap resolved at morning ritual:** "Teacher Exam Authoring UI" means exam authoring
(composing existing bank items into exam-mode assignments), NOT item authoring. ADR-0035
§Decision 2 holds; zero migrations, zero RLS changes.

**Backend scope in S4 (Q-1.1-4.4 resolution):** `CreateAssignmentRequest` extended additively
with `composer_params?: PracticeExamComposerParamsSchema` + `simulation_params?:
SimulationParamsSchema`. assignments-svc handler accepts, persists, and forwards into
`CreateSessionRequest` when student starts the assignment. `difficulty_range` (existing float
range) and `difficulty_distribution` (S2 count-per-band) are acknowledged alternates on the
same type; handler honours whichever is present; refactor deferred.

---

## Constraints

1. TypeScript strict; no `any` in production code.
2. Zod validation at every API boundary.
3. No new migrations; no RLS changes (zero-migration S4 per Q-1.1-4.1 resolution).
4. ADR-0029: all new SDK hooks prepend `/assignments-svc/` or `/content-svc/` prefix.
5. ADR-0035 §Decision 2: no teacher item writes; bank is read-only from teacher UI.
6. UI_CONTRACT: States matrix mandatory on every data-bound component (Loading / Empty /
   Error / 402-upgrade / Content) — merge-blocker.
7. T5 discipline: Checkpoint A (layout sketch) before component code; Checkpoint B (skeleton)
   before filling bodies. Each awaits explicit architect approval.
8. axe-core zero serious/critical on both new routes (DoD per UI_CONTRACT).
9. No Math.random in new code (engine body rule).
10. No Co-Authored-By trailer in commit messages (BUILD_CONTRACT §11.2).
11. Pre-commit secret guard active: `.githooks/pre-commit` rejects real keys.
12. Test floor: 770 → ≥785 (+15 minimum; axe-core tests count).
13. DEV-20260515-2: atomic commit-and-push; announce SHA only after both exit 0.

---

## Deliverables

### D1 — Types: extend `CreateAssignmentRequest`

File: `packages/types/src/assignment.ts`

Add `composer_params?: PracticeExamComposerParamsSchema` + `simulation_params?:
SimulationParamsSchema` as optional fields on `CreateAssignmentRequestSchema`. Import
`PracticeExamComposerParamsSchema` + `SimulationParamsSchema` from `./session.js`.

### D2 — assignments-svc: accept + persist + forward `composer_params` / `simulation_params`

File: `supabase/functions/assignments-svc/handlers.ts`

In `createAssignment` handler: extract `composer_params` + `simulation_params` from validated
request body; persist to `assignments` table column (JSON nullable). In `startAssignment` (or
equivalent session-create forwarder): read persisted `composer_params` + `simulation_params`
from assignment record; include them in the `CreateSessionRequest` forwarded to assessment-svc.

### D3 — SDK hook: extend `useCreateAssignment`

File: `packages/sdk/src/hooks/assignments.ts`

Update `useCreateAssignment` mutation body type to accept `composer_params?` +
`simulation_params?` from the updated `CreateAssignmentRequest`.

### D4 — Route: `/teacher/content` — bank browser page

File: `apps/web/src/app/(teacher)/teacher/content/page.tsx`

Shell: `AppShell variant="teacher"` + `TeacherSidebarNav` + `TopBar` (pattern parity with
`teacher/assignments/page.tsx`). Content: pathway list via `usePathways()`; each row shows
pathway name + item count. States: Loading / Empty / Error / 402-upgrade / Content.
CTA: "New Exam Assignment" → `/teacher/content/new`.

### D5 — Route: `/teacher/content/new` — exam composer form

File: `apps/web/src/app/(teacher)/teacher/content/new/page.tsx`

Single-page form with three section dividers:
1. **Bank Pick** — pathway selector (dropdown from `usePathways()`)
2. **Configure** — item count (5–80), difficulty distribution (easy/mid/hard integer counts
   summing to item_count), time limit (300 000–10 800 000 ms), simulation toggle
   (`no_back_nav` + `hide_feedback_until_submit` both default true)
3. **Assign** — class selector (`useMyClasses()`), due date picker

Submit calls `useCreateAssignment` with the extended body including `composer_params` +
optionally `simulation_params`. States: Loading / Error / Success. BreadcrumbHeader
with `router.back()`.

### D6 — Copy: `/teacher/content` + `/teacher/content/new`

File: `apps/web/src/app/(teacher)/copy/examContent.ts` (new)

Strings for page title, section headers, field labels, error messages, ARIA labels.

### D7 — Sidebar nav: add "Exam Content" entry

File: `apps/web/src/app/(teacher)/teacher/content/page.tsx` + `new/page.tsx`

Extend the `TeacherSidebarNav` `navItems` array to include `{ label: 'Exam Content', href:
'/teacher/content' }` with `active` detection matching the current pathname.

### D8 — Tests (target: +15 minimum)

- `packages/types` — contract tests for updated `CreateAssignmentRequestSchema` (accepts
  `composer_params` + `simulation_params`; rejects invalid; both optional)
- `supabase/functions/assignments-svc` — handler unit tests for `composer_params` /
  `simulation_params` persist + forward
- `packages/sdk` — hook tests for updated mutation body
- `apps/web` — axe-core tests for `/teacher/content` + `/teacher/content/new` routes
  (zero serious/critical; count toward floor)

---

## Verification

Pre-push verification round (per ui-discipline.md §Pre-push verification round V1–V11):

1. **Q-1.1-4.* status verbatim** — all five in ## Resolved.
2. **T1 pre-read citations** — `packages/types/src/assignment.ts` schema fields; `handlers.ts`
   createAssignment signature; `hooks/assignments.ts` mutation body; `teacher/assignments/`
   route shell pattern. All cited verbatim before authorship.
3. **T5 checkpoints** — Checkpoint A sketch approved; Checkpoint B skeleton approved before
   filling bodies.
4. **Test enumeration** — verbatim test names, diff against D8 plan; confirm 770 + ≥15 new ≥785.
5. **Stale-comment grep** — no `PHASE-2:` or `TODO` introduced without ADR.
6. **Mojibake check** — all modified `.md` files.
7. **ADR-0029 SDK prefix grep** — all new hooks use `/assignments-svc/` or `/content-svc/` prefix.
8. **Co-Authored-By absent** — not in prep, impl, or chore commit messages.
9. **Full `pnpm -r run test` output** — not tail-truncated; 0 `.only` / `.skip` introduced.
10. **`pnpm turbo typecheck --force`** — 17/17 packages, 0 cached.
11. **`pnpm -r run lint`** — green.
12. **Secret guard** — `git diff --staged` grep for real-key patterns; none.
