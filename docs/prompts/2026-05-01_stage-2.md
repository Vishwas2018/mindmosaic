# C-C-D-V Prompt — Stage 2 — 2026-05-01

> Executed and approved. Archive per CLAUDE.md evening ritual §8.
> Execution surprises: plan count 66→65, DML-CTE parse error, INSERT raises 42501,
> now() constant in transaction. All documented in DAILY_LOG Stage 2 and ADR-0006.

---

Stage 2 — Migration 0001 — Enums + Tenancy + Auth
Reference: DEV_PLAN.md §2 Stage 2; Arch §1.2–1.3, §1.6, §2.1–2.2, §3.1, §11.1;
Spec §20.2, §20.3.1; G1; G2.

# CONTEXT

- Current phase: Phase 0 — Foundations (Days 1–17, Stages 1–14)
- Day in plan: Day 2
- What exists already that this stage builds on:
  - Turborepo scaffold (Stage 1): turbo.json, pnpm workspaces, apps/web (Next.js 14
    App Router), packages/{types,sdk,ui,core,engines}, CI matrix (lint/typecheck/
    unit/migration-dryrun stub), .husky pre-commit, .gitmessage.
  - Supabase project initialised (config.toml, local stack running).
  - No migrations exist yet.
- What this stage is NOT doing (scope boundaries):
  - No seed data (Stage 14).
  - No Edge Functions (Stage 14).
  - No application auth layer / signup UI (Stage 14).
  - No tables beyond the 7 listed in deliverables.
  - No per-role RLS SELECT granularity within a tenant (deferred to Stage 5 per ADR-0004).

# CONSTRAINTS

- BUILD_CONTRACT.md applies in full. Specifically:
  - §6: No inline subqueries against RLS-enabled tables in USING expressions;
    use SECURITY DEFINER helpers (per ADR-0005).
  - §8: RLS in the same migration as CREATE TABLE — never a follow-up migration.
  - §10: Down migration must reverse cleanly; roundtrip test required.
  - §11.2: Conventional Commits with scope feat(db):.
- Arch §11.1 is authoritative for handle_new_user(); §3.1 simplified version is
  a placeholder. v1 has two branches only: parent auto-create | else RAISE.
- OWNERS.md: auth-svc owns write paths to user_profile, tenant, admin_action_log.
  This migration only defines the tables; no service code this stage.
- No Stripe code anywhere (Stage 42+).
- Phase-2 features (school onboarding, org_admin/teacher invite) absent or
  stubbed with // PHASE-2: comments only.

# DELIVERABLES

Per DEV_PLAN.md Stage 2:

1. `supabase/migrations/0001_enums_tenancy_auth.sql`
   - All custom enums (Arch §2.1): roles/auth, content, session/engine, intelligence,
     jobs, feature flags, assignments, billing, engagement, notifications.
   - Tables (FK dependency order): tenant, user_profile, parent_student_link,
     class_group, class_student, feature_flag (two partial unique indexes per Arch
     §2.2 change #15), admin_action_log.
   - RLS helpers: auth_tenant_id(), auth_user_id(), auth_role() — SECURITY DEFINER,
     STABLE, REVOKE FROM PUBLIC, GRANT TO authenticated.
   - handle_new_user(): G1 parent-only branch — auto-create family tenant + user_profile
     + admin_action_log entry (actor_role='parent', ADR-0003). Else RAISE. PHASE-2
     comment for school onboarding.
   - set_updated_at() trigger on all mutable tables (tenant, user_profile, class_group,
     feature_flag).
   - on_auth_user_created trigger on auth.users.
   - SECURITY DEFINER junction helpers: fn_user_in_my_tenant(uuid),
     fn_class_in_my_tenant(uuid) (ADR-0005).
   - RLS: ENABLE on all 7 tables. Tenant-isolation SELECT policies on 6 tables
     (ADR-0004). Pattern G deny-all on admin_action_log (no policies = deny).

2. `supabase/migrations/down/0001_enums_tenancy_auth.down.sql`
   - Drop trigger → tables (reverse FK order) → functions → enum types.
   - All DROP IF EXISTS for idempotency.

3. `supabase/tests/rls/001_tenancy.sql` — pgTAP test suite.
   - plan(65): G1×7 (RLS enabled), G2×20 (helper shape), G3×1 (trigger exists),
     G4×4 (parent signup functional), G5×1 (non-parent raises), G6×12 (SELECT isolation),
     G7×4 (admin_action_log deny), G8×12 (DML isolation), G9×4 (set_updated_at).

4. `scripts/migration-roundtrip.sh` — up→down→verify→up roundtrip helper.

5. `package.json` — add test:rls and test:migration scripts.

# VERIFICATION

Exit criteria per DEV_PLAN.md Stage 2:

1. `supabase db reset` applies cleanly (migration applies without error).
2. Down migration reverses cleanly; spot-checks confirm all tables and key enum
   types are gone.
3. Migration roundtrip (up→down→verify→up) — all steps pass.
4. pgTAP: plan(65), 65/65 pass.
5. Tenant isolation zero cross-reads on 7 tables (G6, G8).
6. Trigger populates user_profile.tenant_id on parent signup path (G4).

Run in order and report each before claiming completion:
  1. supabase db reset --local
  2. pnpm test:rls
  3. pnpm test:migration

If ANY step fails: stop, surface full output, wait for decision.
