# Stage 12 C-C-D-V Prompt — 2026-05-03

## Context

Stage 12 of MindMosaic v1 MVP (75-day build). Prior stages delivered:
- Stage 1–9: Turborepo monorepo, CI scaffold, Supabase migrations (enums, tenancy, auth,
  content, sessions, intelligence, analytics, assignments, billing, engagement, admin schemas)
- Stage 10: pgTAP RLS test suite (451 tests, 53 tables)
- Stage 11: packages/types — 13 source files, 10 branded ID types, 16 DB enum schemas,
  Zod-first pattern (schemas → inferred types), 97/97 unit tests

Current state: packages/sdk scaffold exists (package.json, tsconfig.json) but src/ is empty.
@mm/types workspace dependency available. SCHEMA_VERSION = '1.0.0'.

## Constraints

- TypeScript strict; no `any` in production code
- BUILD_CONTRACT §9: no external HTTP client library — raw fetch only
- Idempotency-Key header on every POST/PATCH/DELETE (CLAUDE.md quality gate)
- X-Trace-Id: crypto.randomUUID() if not provided; prefer response header echo (arch §1.7)
- X-Client-Version: SCHEMA_VERSION from @mm/types (G3 ambiguity resolution)
- No JSX in SDK package (avoid tsconfig jsx change)
- NodeNext module resolution: .js extensions on all relative imports
- User-approved execution directives (X1–X5):
  - X1: APIError extends Error with code: ErrorCode, status: number, traceId: string
  - X2: SDKResponse<T> = { data: T; traceId: string } on ALL methods; hooks unwrap .data
  - X3: idempotency auto-key via useRef(crypto.randomUUID()); JSDoc warns not retry-safe
  - X4: mmKeys factory with .all()/.byId() hierarchy for invalidation
  - X5: SCHEMA_VERSION drift test in client.test.ts (catches hardcoding)
- Q3 revision: only 5 hook groups (identity, content, session, intelligence, orchestration)
  — no speculative stubs for analytics/assignments/billing/engagement

## Deliverables

1. `packages/sdk/src/client.ts` — MmClient class, APIError, SDKResponse<T>, MmClientConfig
2. `packages/sdk/src/context.ts` — MmClientProvider (createElement, no JSX), useMmClient
3. `packages/sdk/src/keys.ts` — mmKeys query-key factory (7 domains)
4. `packages/sdk/src/hooks/identity.ts` — useMe, useTenant
5. `packages/sdk/src/hooks/content.ts` — usePathways, useAssessmentProfiles
6. `packages/sdk/src/hooks/session.ts` — useCreateSession, useSessionState, useSessionSummary,
   useRecordResponse, useSubmitSession, useCheckpoint
7. `packages/sdk/src/hooks/intelligence.ts` — useBehaviourProfile, useSkillProgress,
   useProficiencyMap, useLearningDNA
8. `packages/sdk/src/hooks/orchestration.ts` — useLearningPlan, usePathwayReadiness,
   usePlanOverride
9. `packages/sdk/src/hooks/index.ts` — barrel re-export
10. `packages/sdk/src/index.ts` — top-level barrel
11. `packages/sdk/src/__tests__/client.test.ts` — 13 tests (incl. X5 drift guard)
12. `packages/sdk/src/__tests__/keys.test.ts` — 9 tests
13. `packages/sdk/src/__tests__/hooks.test.ts` — 2 jsdom plumbing tests (useMe)
14. `docs/dev/decisions/0019-sdk-response-wrapper.md` — ADR-0019 (SDKResponse<T>)
15. Updated package.json (peerDeps: react^18, @tanstack/react-query^5; devDeps: jsdom, RTL@^16)
16. Updated tsconfig.json (lib: ["ES2022", "DOM"])

## Verification

- [ ] pnpm typecheck — 0 errors across all packages
- [ ] pnpm lint — 0 warnings/errors
- [ ] pnpm test — 121/121 pass (97 types + 24 sdk)
- [ ] X5: client.test.ts asserts headers['X-Client-Version'] === SCHEMA_VERSION (import from @mm/types)
- [ ] All POST/PATCH/DELETE hooks carry Idempotency-Key (grep: idempotencyKey)
- [ ] No JSX syntax in packages/sdk/src/ (grep: tsx|jsx)
- [ ] SDKResponse<T> on all 4 MmClient methods (get/post/patch/delete)
- [ ] ADR-0019 status: accepted

## Outcome

All verification gates passed. 24 SDK tests green. ADR-0019 filed and accepted.
Key lint fixes applied during implementation:
- Removed useless try/catch from request() (X1 — network errors propagate naturally)
- Dropped `_data` param from ack schemas: `{ parse: (): void => undefined }` (ESLint no-unused-vars)
- Made children?: ReactNode optional in MmClientProviderProps (createElement TS compatibility)

Commit: 0c3b311
