# C-C-D-V — v1.1-S6 Bulk Content Import Pipeline + Authoring Spec Templates

- Date: 2026-05-19
- Stage: v1.1-S6
- Branch: v1.1/exam-content, HEAD 27ded4d (post doc-chore)
- T5 adapted: backend-artefact gate flow (no UI — Q-1.1-6.5 Option A)

## Context

v1.1-S6 is the first CONTENT OPERATION stage. S1–S5 built the machinery; S6 fills it with tooling.
Content-svc S1 write surface (Zod-validated at b3eb668):

- `createItem` (handlers.ts:838) — `ItemCreateDTOSchema.safeParse`; all new items land as `draft`
- `updateItem` (handlers.ts:877) — `ItemUpdateDTOSchema.safeParse`
- `transitionItemLifecycle` (handlers.ts:1000) — FSM-enforced, 6 edges per spec §15.3
- `createItemVersion` (handlers.ts:925) — atomic `is_current` flip per ADR-0035 §Decision 6

Type schemas in `packages/types/src/content.ts`:
- `ItemCreateDTOSchema` (line 68) — required: `response_type`, `skill_ids`, `difficulty`, `year_levels`, `exam_families`
- `ItemVersionCreateDTOSchema` (line 118) — required: `stem`, `response_config`, `difficulty`
- Lifecycle enum (line 60): `draft | review | active | monitored | retired`

Pattern G strict (ADR-0035 §Decision 1): platform_admin + service-role writes only. Idempotency-Key
on all POST/PATCH per CLAUDE.md non-negotiable.

Six decisions resolved via T3 round-trips (Q-1.1-6.1–6.6, 2026-05-19; ADR-0041 proposed):
- Endpoint: `POST /content/import` per spec §21.2 (not phase plan's `/content/items/batch`)
- Manifest: `copyright_declaration: z.literal('original')` required; `external_key` for per-item dedup
- Dup check: exact-match SHA of normalised stem JSON; fuzzy deferred (ISSUE-0049)
- assessment-svc ISSUE-0042 second half: carry per ADR-0040 (S6 does not touch assessment-svc)
- No UI: T5 adapted to backend artefacts (Gate I / Gate II / Gate III)
- Lifecycle: all imports land as `draft`; no override field in manifest

## Copyright Constraint (VERBATIM — binding, same weight as CLAUDE.md security non-negotiables)

> "NAPLAN and ICAS are real, copyrighted assessment products with protected names. The content
> operation produces **original items only** — written fresh to match the _format, year-level
> curriculum strand, difficulty band, and question type_ of those assessments, without reproducing
> any actual past-paper item. No scraping of past papers, no licensed-bank ingestion, no copying.
> This constraint shapes every part of S6 and S7. It is not optional and not negotiable; treat it
> with the same weight as the child-safety and security non-negotiables in CLAUDE.md."

## Constraints

- Branch v1.1/exam-content only
- COPYRIGHT CONSTRAINT verbatim above — binding on all S6 code and docs
- T5 three-gate adapted (no UI): Gate I / Gate II / Gate III — three operator "proceed" approvals
- `POST /content/import` path non-negotiable (spec §21.2; Q-1.1-6.2)
- Pattern G strict (platform_admin + service-role) on new route (ADR-0035 §Decision 1)
- Idempotency-Key on POST (CLAUDE.md non-negotiable)
- 0 new migrations expected (batch import writes through existing schema via service-role)
- Dry-run mode (`?dry_run=true`) required per spec §21.2
- Legal review of authoring spec templates: hard gate for S7.1; owner = operator-side (ADR-0041 §Decision 4)
- All imports land as `draft`; no lifecycle override in manifest (ADR-0041 §Decision 6)
- `copyright_declaration: z.literal('original')` required manifest field; 422 on missing (ADR-0041 §Decision 2)
- Exact-match SHA dup check on normalised stem JSON; fuzzy deferred ISSUE-0049 (ADR-0041 §Decision 3)
- Per-item `external_key` field for idempotency dedup (survives re-import of same manifest)
- Partial-failure: never silent half-import; every manifest item appears in response with status
- Never `--amend` over a pushed commit (T4)
- Pre-commit secret guard active (.githooks/pre-commit); atomic commit-and-push per DEV-20260515-2
- No `.only`/`.skip` in tests; no stale `TODO`/`FIXME`/`PHASE-2:` comments

## Deliverables

Gate I (API design sketch — no code):
- `docs/content/manifest-format.md` — versioned manifest JSON spec (draft)
- ADR-0041 outline reviewed (status: proposed at prep commit)

Gate II (skeleton — no business logic):
- `ItemImportManifestSchema` in `packages/types/src/content.ts` (Zod schema for manifest item)
- `importItems` handler stub in `supabase/functions/content-svc/handlers.ts` (signature + Zod parse + throw stubs)
- Router branch for `POST /content/import` in `supabase/functions/content-svc/index.ts`
- Contract test stubs in `supabase/functions/content-svc/__tests__/contract.test.ts`

Gate III (fill — full implementation):
- `importItems` handler fully implemented: Zod parse, external_key idempotency dedup, SHA dup check,
  dry-run mode, partial-failure loop, DB writes via `createItem` call pattern
- Contract tests ≥5: happy path, partial failure, duplicate rejection (SHA), schema violation
  (`copyright_declaration` missing), idempotent re-run (same external_key twice)
- `docs/content/specs/` — authoring spec templates (one per assessment × year × subject strand;
  copyright guardrails "original items only; alignment not reproduction" included)
- `docs/content/manifest-format.md` — finalised versioned spec
- ADR-0041 status updated to `accepted`
- OPEN_ISSUES.md + PROJECT_STATE.md updated at chore close

## Verification (Gate III pre-push)

- pnpm turbo typecheck --force: 17/17, 0 cached
- pnpm -r run lint: 7/7 green
- pnpm -r run test: 831+ passed, no regression; content-svc contract tests include ≥5 import scenarios
- V1–V11 pre-push verification round
- `copyright_declaration` field present in `ItemImportManifestSchema`
- `POST /content/import` path confirmed in index.ts router
- Dry-run mode confirmed in handler
- No migrations added (or deviation logged if required)
- ADR-0041 status = accepted
- Legal review gate recorded in ADR-0041 (no code enforcement)

## T5 Gate Flow

- Gate I complete when: `docs/content/manifest-format.md` draft + ADR-0041 outline surfaced →
  operator "proceed to checkpoint B"
- Gate II complete when: skeleton files staged + test stubs → operator "proceed to fill"
- Gate III complete when: full impl + V1–V11 passed → operator "create the commit"
