# Stage 14 C-C-D-V Prompt — 2026-05-04

## Context

MindMosaic v1 Stage 14. Phase 0 (foundation). All 13 prior stages complete. Schema
migrations 0001–0010 applied, packages @mm/types/@mm/core/@mm/engines/@mm/sdk/@mm/ui all
built and tested. Goal: wire up the full frontend + backend scaffolding and seed the database
with G5-compliant content so the product is ready for engine implementation (Stage 15+).

Prerequisite state:
- @mm/ui has design tokens + 26 primitives + axe gate (Stage 13)
- Supabase schema complete through migration 0010
- No apps/web yet; no Edge Functions yet; no seed data yet

## Constraints

- G1: parent-only self-signup; student accounts invite-only
- G2: pre-Stripe; feature_flag managed via set-tenant-tier CLI or seed only
- G5 content minimum: 50 items (25 NAPLAN + 25 ICAS), 10 misconceptions, 2 stimuli,
  9 skill nodes, 4 prerequisite edges; 15/20/15 difficulty distribution
- TypeScript strict: no `any` in production; noPropertyAccessFromIndexSignature active
- BUILD_CONTRACT §11.2: no "AI Co-Authored-By" trailers in commits
- Atomic commits: one cluster = one commit

## Deliverables

### Cluster A — apps/web scaffold
- Next.js 14 App Router: 5 route groups, server components, layout.tsx with DM Sans + tokens
- src/middleware.ts: JWT role guard; unauthenticated → /login; wrong-role → role home
- lib/supabase/{client,server}.ts: @supabase/ssr helpers
- AuthProvider + EntitlementsProvider (PHASE-2 stub) + Providers
- LoginForm + SignupForm: react-hook-form + zod
- AuthPageShell: two-panel (white form + purple brand)
- Dashboard placeholder pages per role (EmptyState + "Available in a future release")

### Cluster B — Edge Functions + migration 0011
- supabase/functions/auth-svc: 6 endpoints with rate limiting + G1 guard
- supabase/functions/users-svc: 4 endpoints including parent/student link
- supabase/functions/_shared: trace-id, error-envelope, rate-limit, auth, logger
- supabase/migrations/0011: fn_check_rate_limit RPC + fn_cleanup_outbox + outbox.cleanup cron

### Cluster C — Seeds + Scripts + CI
- supabase/seeds/01–06.sql: skill graph, content (50 items), assessment config, users, flags, subs
- scripts/validate-content.ts: G5 assertion script
- scripts/set-tenant-tier.ts: CLI tier manager (G2 authorised writer)
- CI seed-file-count lint check

## Verification

- pnpm turbo run typecheck → 0 errors across all 6 packages
- pnpm turbo run lint → 0 errors across all 6 packages
- Pre-commit hook passes 12/12 for Cluster A, 12/12 for Cluster B, 12/12 for Cluster C
- seed sql_paths glob picks up all 6 files
- pnpm exec tsc --noEmit --project scripts/tsconfig.json → 0 errors
- ISSUE-0004 resolved; 0 open issues after close

## Outcome

All three clusters committed (5e3e1f0, c3df874, 969ec57). FULL TURBO on all commits.
Phase 0 foundation complete. Stage 15 (engines-client) is unblocked.
