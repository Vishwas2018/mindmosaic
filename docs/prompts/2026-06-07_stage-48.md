# Stage 48 C-C-D-V — Hardening Pass

**Date:** 2026-06-07
**Stage:** 48 — Hardening Pass
**Budget:** 4 days (Days 72–75 per DEV_PLAN; actual Day 65 — 8 days ahead on +7.5 buffer)

---

## §2A Pre-Read Findings (R1-R14)

**R1 — PROJECT_STATE (Stage 47 close state):**
- Last completed: Stage 47 (2026-06-06). 696/697 Vitest. 0/0/8/13 issues. 10 open deviations.
- +7.5 days buffer. Tags: v1-phase-1, v1-phase-2-partial, v1-phase-4-partial on origin.
- Backup branch: codespace-stage-47-backup @ 30d8b17.
- Stage 48 notes from Stage 47 close: Docker migrations, k6, Playwright, soak, axe-core, validate-content, pnpm audit.

**R2 — DEV_PLAN Stage 48 (verbatim):**
> "Stage 48 — Day 72 — Hardening Pass. Objective: Last-mile fixes before launch. Deliverables: Review DAILY_LOG.md blockers — resolve any carried-over issues; Run full axe-core sweep across all routes — fix any regression; k6 soak at Phase 1 SLA (500 concurrent) for 1 hour — verify no degradation; Check pipeline.dead_letter.count over 24h — must be 0; Run scripts/validate-content.ts — verify seed integrity; Verify all .env.example keys present (including Stripe now); Supabase backup drill — restore to staging, verify data intact; All CI jobs green on 3 consecutive commits. Exit criteria: Zero blockers. All checks green."

Note: DEV_PLAN says **500 concurrent** — not 1000.

**R3 — DEV_PLAN Stage 49 (verbatim):**
> "Stage 49 — Day 73 — Launch Gate Review + v1.0.0 Tag. Objective: Ship v1. Deliverables: PROJECT_STATE final snapshot; Final DAILY_LOG entry; Launch gate checklist: (1) All Phase 4 slice exit criteria met ✅; (2) Load test at 500 concurrent green for 1 hour ✅; (3) pipeline.dead_letter.count = 0 for 24h ✅; (4) Backup + restore drill completed ✅; (5) Stripe taxes + invoicing verified in test mode ✅; (6) Content seeded: 50 items, 10 misconceptions ✅; git tag -a v1.0.0 + push. Exit criteria: Tagged. Deployable. Known deferrals documented."

Note: DEV_PLAN §49 says "all 47 stages ✅" — will be 49/75 at actual close. Pre-written artefact.

**R4 — BUILD_CONTRACT §10 SLAs (8 budgets):**
- §8 rule (line 99): Stripe webhook p95 ≤ 300ms
- §10 table: Item delivery ≤200ms · Session create ≤1s · Session respond ≤300ms · Session submit ≤5s · Pipeline sync ≤3s · Pipeline async ≤30s · Dashboard load ≤2s
- 7 in §10 table + 1 Stripe webhook from §8 = 8 total. Flag propagation p95 ≤30s = same as Pipeline async budget.

**R5 — Pre-Deploy Checklist carry-forward:**
- 16 open items combined (Phase 2 §9 + Phase 4 §9 additions).
- 5 sandbox-achievable: pnpm audit, validate-content.ts, .env.example audit, git hooks activation, 3-consecutive-commits.
- 11 environment-gated: Docker migrations, k6 execution, Playwright, 24h soak, Stripe E2E, backup drill, 8 SLA measurements.

**R6 — Sandbox capability check:**
- Docker: ✅ daemon running (Server 29.3.1, 11 containers active)
- supabase CLI: ❌ not in PATH; `npx supabase@2.98.2` available as fallback (Q-48.2)
- k6: ❌ binary not in PATH; scripts exist (Q-48.3)
- Playwright: ✅ binary in apps/web/node_modules/.bin/; browser install needed; deployed Supabase required
- axe-core: ❌ no standalone CLI; integrated as library in Vitest unit tests (Stage 13+)
- psql / pg_isready: ❌ no Postgres client in PATH

**R7 — Migrations 0012–0020 triage:**
| Migration | Type | Deploy constraint |
|---|---|---|
| 0012 | RPC function widen | None |
| 0013 | ADD VALUE 'behaviour_signal' | One-way DDL |
| 0014 | dead_lettered_at + fn_pickup_jobs | None |
| 0015 | pathway_id column on assignment | None |
| 0016 | fn_drain_outbox_batch OR REPLACE | None |
| **0017** | ADD VALUE 'manual' to alert_type | **One-way + before analytics-svc** |
| 0018 | 4 billing tables | None |
| **0019** | user_role 'system' + sentinel rows | **One-way + before billing-svc** |
| **0020** | ADD VALUE 'access_downgraded' | **One-way + before notifications-svc** |
Last pgTAP run: 451/451 against 0001–0013 (2026-05-03). Migrations 0014–0020 untested.

**R8 — Routes: 21 confirmed** — 6 public, 6 student, 2 parent, 6 teacher, 1 admin. axe-core sweep = 21 targets.

**R9 — Stripe prerequisites:** 7 env vars required. scripts/stripe-seed.sh present. billing-cancel.spec.ts needs live Stripe test env + deployed billing-svc. **Not achievable in sandbox.**

**R10 — Playwright spec inventory:**
- 11 spec files on disk (all in apps/web/playwright/e2e/) — all test.skip-guarded, all require deployed Supabase.
- Q-48.5 resolved: Phase 2/4 exit reports overcounted by 2 (documentation error). On-disk ground truth = 11 specs / 15 tests. ISSUE-0035 at close.

**R11 — k6 scripts:**
- k6/session-loop.js: present (500 VU, create/respond/submit). k6 binary: absent.
- Gap: no billing k6 script. Q-48.3 resolved: author k6/billing-webhook.js at impl.

**R12 — 24h soak:** Codespace cannot sustain 24h. Q-48.4 resolved: defer entirely. Stage 49 launch gate item 3.

**R13 — Deviations triage:** 0 resolvable in Stage 48. DEV-20260606-2 filed this prep commit. All others v1.1 or ongoing.

**R14 — DEV-20260606-2:** Not pre-filed (G16 = 0 hits). Filed in this prep commit per roadmap.

**Q-48.* resolutions (operator confirmed 2026-06-07):**
- Q-48.1: **Conditional Go sandbox subset** — Phase 1/2/4 precedent. Deployed-env items defer to Stage 49.
- Q-48.2: **Attempt `npx supabase@2.98.2`** after version check. Fallback: defer to deployed env.
- Q-48.3: **Author `k6/billing-webhook.js`** as deploy-ready artefact. No execution.
- Q-48.4: **Defer 24h soak entirely.** No proxy. Stage 49 item 3.
- Q-48.5: **11 specs on disk = ground truth.** Phase 2/4 exit reports overcounted +2. ISSUE-0035 at close.

---

## Scope Partition Table

| Item | Partition | Stage 49 gate? |
|---|---|---|
| DAILY_LOG blockers review | SANDBOX | No |
| DEV-20260606-2 filed (this prep commit) | SANDBOX | No |
| pnpm audit → security/findings.md | SANDBOX | Yes (pre-deploy checklist) |
| .env.example audit (Stripe keys) | SANDBOX | Yes (pre-deploy checklist) |
| scripts/validate-content.ts | SANDBOX | Yes (launch gate item 6) |
| axe-core sweep (via existing test suite) | SANDBOX | No |
| 3-consecutive-commits criterion | SANDBOX | Yes (Stage 48 exit criterion) |
| Docker migrations 0012–0020 + pgTAP | SANDBOX (npx supabase) | Yes (pre-deploy checklist) |
| k6/billing-webhook.js authored | SANDBOX | Yes (unblocks k6 execution at Stage 49) |
| stage-48-exit-report.md | SANDBOX | Yes |
| k6 1h 500-VU soak execution | REQUIRES-DEPLOYED | Yes (launch gate item 2) |
| 8 SLA numerical measurements | REQUIRES-DEPLOYED | Yes (perf budgets) |
| Playwright 11 specs / 15 tests | REQUIRES-DEPLOYED | Yes (launch gate) |
| billing-cancel.spec.ts + Stripe | REQUIRES-DEPLOYED | Yes (launch gate) |
| 24h pipeline.dead_letter.count soak | REQUIRES-DEPLOYED | Yes (launch gate item 3) |
| Supabase backup drill | REQUIRES-DEPLOYED | Yes (launch gate item 4) |
| Stripe test mode invoicing verification | REQUIRES-DEPLOYED | Yes (launch gate item 5) |

---

## C — Context

Stage 48 = Hardening Pass. Last-mile checks before Stage 49 (v1.0.0 tag). 4-day budget per DEV_PLAN; actual Day 65 — 8 days ahead on +7.5 buffer. No feature delivery. Conditional Go verdict applies per Phase 1/2/4 precedent — sandbox-achievable items proceed; deployed-env items documented as deferred to Stage 49 launch-window operational verification. Docker available (daemon running). k6/Playwright/Stripe/Supabase deployed env not available. axe-core integrated in existing Vitest suite (not a standalone CLI). 11 Playwright specs all gated on deployed env.

---

## C — Constraints

- CLAUDE.md anti-pattern 1: no active DEV_PLAN stage definitions touched
- Conditional Go verdict confirmed (Q-48.1): Stage 48 closes when sandbox items pass
- T4: no --amend on pushed commits; fix-then-push
- Pre-push round MANDATORY; push gate per commit
- DEV-20260606-2 filed this prep commit (done)
- Q-48.5 resolved: 11 specs on disk = truth; ISSUE-0035 at close (T2-tightened)
- No Stripe env vars in this environment — skip Stripe-dependent items entirely
- Docker migrations: attempt `npx supabase@2.98.2`; version-check first; log outcome regardless
- axe-core: run existing axe-instrumented Vitest tests (not a standalone CLI sweep); confirm no regression
- commit message: NO Co-Authored-By (BUILD_CONTRACT §11.2 + commit-msg hook)

---

## D — Deliverables

**Prep commit (this commit):**
- D1: `docs/dev/QUESTIONS.md` — Q-48.1..5 in ## Resolved
- D2: `docs/dev/DEVIATIONS.md` — DEV-20260606-2 prepended
- D3: `docs/prompts/2026-06-07_stage-48.md` — this C-C-D-V

**Impl work (separate impl commit):**
- D4: `k6/billing-webhook.js` (NEW) — deploy-ready billing SLA script (webhook p95 + flag propagation p95)
- D5: `docs/dev/security/findings.md` — `pnpm audit` output logged
- D6: `.env.example` audit — verify all Stripe + service URL keys present; patch if missing (no code changes expected)
- D7: `scripts/validate-content.ts` run — log output + exit code
- D8: axe-core run — confirm no regression (test run output captured)
- D9: Docker migration run — `npx supabase@2.98.2 start && db reset && test db`; log pgTAP result or fallback-defer note

**Close commit:**
- D10: `docs/dev/stage-48-exit-report.md` (NEW) — 10 sections, Conditional Go verdict, scope partition table, §5 deployed-env deferrals, §9 updated pre-deploy checklist
- D11: `docs/dev/OPEN_ISSUES.md` — ISSUE-0035 filed (Playwright spec count documentation error, low severity)
- D12: `docs/dev/DAILY_LOG.md` — Stage 48 entry prepended
- D13: `docs/dev/PROJECT_STATE.md` — overwritten (Stage 49 next, Day 65+N/75, updated pgTAP row if Docker run succeeded)
- D14: pnpm install + pnpm turbo typecheck --force (close-ritual)

---

## V — Verification

```
# Prep commit
grep 'Q-48\.[12345]' docs/dev/QUESTIONS.md → 5 hits in ## Resolved
grep 'DEV-20260606-2' docs/dev/DEVIATIONS.md → ≥1 hit
grep 'npx supabase' docs/dev/QUESTIONS.md → ≥1 hit (Q-48.2)
git diff DEV_PLAN.md → empty

# Impl
ls k6/billing-webhook.js → exists
grep 'p(95)<300' k6/billing-webhook.js → ≥1 hit (webhook budget)
grep 'p(95)<30000' k6/billing-webhook.js → ≥1 hit (async pipeline budget)
cat docs/dev/security/findings.md → pnpm audit output
validate-content.ts exit 0
axe: pnpm -r run test → 696/697 unchanged (no regression)

# Close
grep '^## ' docs/dev/stage-48-exit-report.md | wc -l → 10
grep 'Conditional Go' docs/dev/stage-48-exit-report.md → ≥1 hit
grep 'ISSUE-0035' docs/dev/OPEN_ISSUES.md → ≥1 hit
grep '696' docs/dev/PROJECT_STATE.md → ≥1 hit (unchanged tests)
pnpm install && pnpm turbo typecheck --force → 17/17, 0 cached
pnpm -r run test → 696/697 unchanged
mojibake check on all modified .md files
Co-Authored-By absent in all commit messages
```
