# Stage 41 — Phase 2 Exit Review — C-C-D-V Prompt
# Date: 2026-05-31

---

## Context

Stage 41 is the Phase 2 (Stages 28–41) audit and gate stage. No feature delivery.
Phase 2 delivered: L3b/L5/L7/L9 async pipeline (jobs-worker + 4 domain services),
analytics-svc, orchestration-svc, notifications-svc, assignments-svc, parent dashboard,
teacher dashboard, teacher student detail, assignment engine (teacher UI), student
assignments + dashboard v2. 13 stages shipped. 593 Vitest + 3 Playwright gated.
ADRs 0001–0033 all accepted. 0 open Q-*. 10 open deviations (all v1.1 or acceptable
process debt). Issues: 0/0/9/12.

T3 round-trips for Stage 41 discharged in prep commit:
- Q-41.1: Option A — Conditional Go (sandbox-partial exit; numerical SLAs → Stage 48)
- Q-41.2: Option A — push v1-phase-1 + v1-phase-2-partial tags at Stage 41 close
- Q-41.3: Option C — CLAUDE.md §T-Discipline summary + docs/dev/ui-discipline.md detail

T-discipline in force: T1/T2/T3/T4/T5 (see discipline timeline in morning ritual report).
Stage 41 is docs-only — no code changes. Pre-push verification applies to docs artefacts.

---

## Constraints

- No code changes. Docs-only stage. All commits are `chore()` scoped.
- No Co-Authored-By trailers (BUILD_CONTRACT §11.2, pre-commit hook enforced).
- Push gate: architect "create the commit" approval required separately for each
  of the two commits (Commit 1 canonisation; Commit 2 audit report). Two tag-push
  ops also require separate approval each.
- CLAUDE.md §T-Discipline addition must not bloat CLAUDE.md beyond ~15 lines for
  the new section. Detail belongs in ui-discipline.md.
- phase-2-exit-report.md mirrors phase-1-exit-report.md structure verbatim (10
  sections: Summary, Phase 2 Deliverables, Quality Gates, Performance, Open Issues,
  Deviations, ADR Inventory, Pre-Deploy Checklist, Statistics, Verdict).
- Numerical SLAs recorded as "not measured — requires deployed environment (Stage 48)"
  exactly as Phase 1 precedent. Do not invent numbers.
- OPEN_ISSUES.md triage: audit-day prune (every 5 stages; Stage 40 = Stage 35 + 5).
  Move resolved to ## Resolved. Confirm v1.1 target on all medium/low items.
  Do NOT change severity without documented rationale.
- ISSUE-0029 process fix (close-ritual cache bust) must be documented in the
  CLAUDE.md §Evening ritual as `pnpm turbo typecheck --force` — not just noted
  in OPEN_ISSUES.md.
- deployment.md must list all 5 service URL env vars with fallback logic + migration
  0017 deploy-order note. No invented values.

---

## Deliverables

### Commit 1 — chore(stage-41): canonise T-discipline + deployment docs

D1. `CLAUDE.md` — add §T-Discipline section (T1–T5 5-bullet rule summary + push-gate
    protocol + close-ritual cache-bust). Place after §Quality non-negotiables.
    Also amend §Evening ritual: add step to run `pnpm turbo typecheck --force`
    before committing (ISSUE-0029 fix; DEV-20260527-1).

D2. `docs/dev/ui-discipline.md` (NEW) — full T1–T5 rationale + per-rule Stage
    precedent history from discipline evolution timeline (Stage 28–40 events).
    Sections: T1 (pre-read DTO schemas verbatim), T2 (Q-* filing timing),
    T3 Option 3 hybrid (structural vs detail threshold), T4 (no amend on pushed),
    T5 (mid-impl skeleton checkpoint). Each rule: definition, origin stage, precedent
    evidence chain (DAILY_LOG stage refs), and the canonical do/don't form.

D3. `docs/dev/deployment.md` (NEW) — env var table (5 service URL vars:
    INTELLIGENCE_SVC_URL, ANALYTICS_SVC_URL, ORCHESTRATION_SVC_URL,
    ASSESSMENT_SVC_URL, NOTIFICATIONS_SVC_URL; each with fallback resolution logic);
    migration 0017 deploy-order note (ALTER TYPE alert_type ADD VALUE — one-way DDL,
    must run before analytics-svc code that inserts alert_type='manual' is deployed);
    migrations 0012–0016 Docker requirement note; .githooks activation note.
    Closes ISSUE-0018. Resolves DEV-20260524-1 (deploy-gate documentation side).

### Commit 2 — chore(stage-41): phase-2 exit report + triage + close docs

D4. `docs/dev/phase-2-exit-report.md` (NEW) — mirrors phase-1-exit-report.md
    structure verbatim:
    §1 Summary + Verdict (Conditional Go);
    §2 Phase 2 Deliverables (services, SDK, UI, pipeline, dashboard);
    §3 Quality Gates (CI gates table + test breakdown per package + 3-consecutive-
       commits criterion);
    §4 Performance vs BUILD_CONTRACT §10 (all 4 SLAs: "not measured — Stage 48");
    §5 Open Issues at Phase 2 Close (all medium severity; v1.1 targets);
    §6 Phase 2 Technical Deviations (open 10; resolved per-stage);
    §7 Phase 2 ADR Inventory (ADR-0031 through ADR-0033; total at close = 33);
    §8 Pre-Deploy Checklist (migration 0012–0017 Docker; ISSUE-0018 env vars;
       migration 0017 deploy order; numerical SLAs; Playwright E2E; tag push ops);
    §9 Phase 2 Statistics (stages, dates, buffer, test delta, ADRs, issues);
    §10 Verdict.

D5. `docs/dev/OPEN_ISSUES.md` — audit-day triage:
    - ISSUE-0013 (low, process): move to ## Resolved (fixed from Stage 29 — full
      `pnpm test` output captured per stage since Stage 28 close). Resolution date 2026-05-31.
    - ISSUE-0026 (low → already resolved Stage 40): confirm entry is in ## Resolved.
    - All remaining open issues: confirm v1.1 target tags are accurate; no severity changes.
    - No new issues opened in this triage (audit day, not implementation).

D6. `docs/dev/DAILY_LOG.md` — prepend Stage 41 entry using template.

D7. `docs/dev/PROJECT_STATE.md` — overwrite with fresh snapshot:
    - Last completed: Stage 41 (Phase 2 Exit Review, Conditional Go)
    - Next stage: Stage 42 — Stripe Integration + Webhook (Phase 4 slice)
    - Tests: 593 Vitest / 1 skipped + 3 Playwright gated (unchanged — docs-only stage)
    - Quality gates: lint/typecheck/test re-run with `pnpm turbo --force` (eat own dog food)
    - Stages closed: 41/75

---

## Verification

### V1 — Q-41.* entries in QUESTIONS.md ## Resolved

```
grep -n "Q-41\." docs/dev/QUESTIONS.md
```
Expect: 3 hits (Q-41.1, Q-41.2, Q-41.3), all in ## Resolved section.

### V2 — CLAUDE.md §T-Discipline present + cache-bust in evening ritual

```
grep -n "T-Discipline\|turbo typecheck --force" CLAUDE.md
```
Expect: ≥1 hit for "T-Discipline" (section heading), ≥1 hit for "turbo typecheck --force" (evening ritual amendment).

### V3 — ui-discipline.md exists and references all 5 T-rules

```
grep -n "^## T[1-5]" docs/dev/ui-discipline.md
```
Expect: 5 section headings (T1 through T5).

### V4 — deployment.md exists with all 5 env vars

```
grep -n "INTELLIGENCE_SVC_URL\|ANALYTICS_SVC_URL\|ORCHESTRATION_SVC_URL\|ASSESSMENT_SVC_URL\|NOTIFICATIONS_SVC_URL" docs/dev/deployment.md
```
Expect: 5 hits.

### V5 — phase-2-exit-report.md exists with Conditional Go verdict

```
grep -n "Conditional Go\|v1-phase-2-partial" docs/dev/phase-2-exit-report.md
```
Expect: ≥2 hits.

### V6 — OPEN_ISSUES.md ISSUE-0013 in ## Resolved

```
grep -n "ISSUE-0013" docs/dev/OPEN_ISSUES.md
```
Expect: hit in ## Resolved section (not ## Open).

### V7 — No invented numbers in phase-2-exit-report.md

```
grep -n "not measured" docs/dev/phase-2-exit-report.md
```
Expect: ≥4 hits (one per SLA endpoint).

### V8 — pnpm turbo typecheck --force clears green

Run: `pnpm turbo typecheck --force`
Expect: all 16 packages green, no cache artefact.

### V9 — pnpm -r run lint green

Run: `pnpm -r run lint`
Expect: green (docs-only changes; no lint-relevant file modifications).

### V10 — No mojibake in modified .md files

```
grep -Pn "[^\x00-\x7F]" CLAUDE.md docs/dev/ui-discipline.md docs/dev/deployment.md docs/dev/phase-2-exit-report.md docs/dev/QUESTIONS.md docs/dev/OPEN_ISSUES.md docs/dev/DAILY_LOG.md docs/dev/PROJECT_STATE.md
```
Expect: 0 hits (all ASCII).

### V11 — Tag state pre-push

```
git tag -l "v1-phase-*"
```
Expect: `v1-phase-1` listed (created Stage 27). `v1-phase-2-partial` to be created at Stage 41 impl commit before tag-push approval.

---

## Commit strategy

1. **Commit 1** (canonisation): CLAUDE.md + ui-discipline.md + deployment.md. Surface diff → architect "create the commit" approval → commit.
2. **Commit 2** (audit): phase-2-exit-report.md + OPEN_ISSUES.md triage + DAILY_LOG + PROJECT_STATE.md. Surface diff → architect "create the commit" approval → commit.
3. **Tag create** (post-Commit 2): `git tag v1-phase-2-partial` → surface → operator "create the commit" approval → tag.
4. **Tag push v1-phase-1** → operator approval → `git push origin v1-phase-1`.
5. **Tag push v1-phase-2-partial** → operator approval → `git push origin v1-phase-2-partial`.
6. **Commit push** (for commits 1+2) → already approved per-commit; push both together post-tag approval.

Commit messages (no Co-Authored-By):

Commit 1:
```
chore(stage-41): canonise T-discipline rules + deployment docs

CLAUDE.md §T-Discipline (5-rule summary + push-gate protocol +
close-ritual cache-bust). docs/dev/ui-discipline.md (new — full
T1-T5 rationale + Stage 28-40 precedent history). docs/dev/
deployment.md (new — 5 service URL env vars + migration 0017 deploy
order). Closes ISSUE-0018. Documents DEV-20260527-1 process fix.
```

Commit 2:
```
chore(stage-41): phase-2 exit report + audit triage + close docs

docs/dev/phase-2-exit-report.md (Conditional Go — Q-41.1 Option A).
OPEN_ISSUES.md audit-day triage (ISSUE-0013 resolved). PROJECT_STATE
Stage 42 next. Tags: v1-phase-1 (push, was local since Stage 27) +
v1-phase-2-partial (new).
```
