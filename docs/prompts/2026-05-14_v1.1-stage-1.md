# v1.1 Stage 1 C-C-D-V — Question Bank Foundation

**Date:** 2026-05-14
**Stage:** v1.1-S1 (branch: v1.1/exam-content)

---

## Context

v1 build window closed 2026-06-07 (Day 65 of 75) at tag v1.0.0 (SHA 9376d98). Branch
`v1.1/exam-content` created from v1.0.0 for the exam-content authoring phase (5 stages).

Stage 1 premise (operator confirmed): the `item`, `item_version`, and `stimulus` tables already
exist from migration 0002 (`0002_content_skill_graph.sql`). Stage 1 delivers write-side CRUD on
the existing schema — no new tables or enum values required.

Architect resolutions (binding, 2026-05-14):
- Q-1.1-1.0: Accept exam-content as v1.1 re-prioritization. DEV-20260514-1 filed.
- Q-1.1-1.1: No new columns. Author attribution via `item_version.metadata.author_id`.
- Q-1.1-1.2: Spec §15.3 verbatim. 6 legal FSM edges; `draft→retired` excluded.
- Q-1.1-1.3: Pattern G strict. Writes = `platform_admin` + `service-role` only.
- Q-1.1-1.4: Platform-only Stage 1. Teacher authoring deferred.
- Q-1.1-1.5: Resolved by spec §5.3 — existing `skill_ids`/`exam_families`/`year_levels` columns.
- Q-1.1-1.6: Resolved by spec §5.2 line 834 — lifecycle enum is the retire mechanism; no hard-delete.

T1 pre-reads completed (2026-05-14):
- spec §15.3 verbatim (lines 2195–2210): 6 edges confirmed, `draft→retired` absent
- migration 0001 line 48: `item_lifecycle ENUM ('draft', 'review', 'active', 'monitored', 'retired')`
- migration 0002 line 176: `item.lifecycle item_lifecycle NOT NULL DEFAULT 'draft'`
- migration 0002 line 197: `item_version.metadata jsonb NOT NULL DEFAULT '{}'`
- migration 0002 lines 209–210: `idx_item_version_current_one UNIQUE ON item_version(item_id) WHERE is_current = true`

---

## Constraints

- Branch: `v1.1/exam-content` (from v1.0.0)
- No new migrations in Stage 1 — write on existing schema only
- Pattern G strict: platform_admin + service-role writes (ADR-0035)
- Spec §15.3 verbatim FSM: 6 edges, no `draft→retired`
- Idempotency-Key on all POST/PATCH endpoints (arch §4.8)
- Atomic is_current flip via `idx_item_version_current_one` constraint (migration 0002)
- T1–T5, push gate, pre-push verification round, close-ritual cache-bust all in force
- No Co-Authored-By (BUILD_CONTRACT §11.2 + commit-msg hook)
- CLAUDE.md anti-pattern 1: active DEV_PLAN stage definitions immutable

---

## Deliverables

**Prep commit (docs only):**
- `docs/dev/QUESTIONS.md` — Q-1.1-1.0..6 added to ## Resolved
- `docs/dev/DEVIATIONS.md` — DEV-20260514-1 prepended
- `DEV_PLAN.md` §5.1 — additive entry for v1.1 exam-content phase (≤30 lines)
- `docs/dev/decisions/0035-content-authoring-write-model.md` — ADR-0035 (proposed)
- `docs/prompts/2026-05-14_v1.1-stage-1.md` — this file

**Impl commit (code):** TBD — operator will relay impl prompt after prep lands on origin.

---

## Verification (prep commit)

V1  Q-1.1-1.0..6 all resolved and filed in QUESTIONS.md ## Resolved
V2  T1 pre-reads: spec §15.3 + migration 0002 schema anchors cited verbatim above
V3  No production code changed — test enumeration N/A
V4  No production files modified — stale-comment grep N/A
V5  Mojibake: all modified .md files decode as valid UTF-8
V6  ADR-0029 SDK prefix: no hook paths modified — N/A
V7  Co-Authored-By absent from commit message
V8  No test run needed (no code change); pnpm -r run test baseline: 696/697 (v1.0.0)
V9  pnpm install && pnpm turbo typecheck --force → 17/17, 0 cached
V10 pnpm -r run lint → 17 packages green
V11 grep checks per operator spec
